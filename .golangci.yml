version: v2
run:
  tests: true
  timeout: 5m
  modules-download-mode: mod

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused
  disable:
    - typecheck

issues:
  exclude-use-default: false