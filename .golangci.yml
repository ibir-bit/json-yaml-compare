cat > code/.golangci.yml << 'EOF'
version: "1"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - unused

run:
  timeout: 5m

issues:
  exclude-use-default: false
EOF